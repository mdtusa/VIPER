@model VIPER.Models.ViewModel.LocationViewModel

@(Html.Kendo().DropDownList().Name("Location")
    .OptionLabel("Select...")
    .DataTextField("Name")
    .DataValueField("LocationID")
    .DataSource(d => d
        .Read(r => r.Action("JSONLocation", "Admin"))
    )
)

